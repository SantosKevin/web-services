export class Texto {
    source: string;
    target: string;
    input: string;

    Texto(source?: string, target?: string, input?: string){
        this.source = source;
        this.target = target;
        this.input = input;
    }
}
